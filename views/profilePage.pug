extends layouts/main-layout.pug

block content   
    if !profileUser
        h2.errorMessage Check the URL you are trying to access.
    else 
        span  #{profileUser.firstName}
        .postsContainer

    +createPostModals(userLoggedIn)

block scripts
    script(src="/js/profile.js")